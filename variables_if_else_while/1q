#include <stdio.h>
/**
 * main - Entry point
 * Description: Program that prints all single digit numbers of base 10 from 0.
 * Return: 0
 */
int main(void)
{
	int x = 0;

	for (x < 10)
		printf("%d\n", x);
	x==;
return (0);
}
