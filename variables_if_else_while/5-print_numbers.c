#include <stdio.h>
/**
 * main - Entry point
 * Description: Program that prints all single digit numbers of base 10 from 0.
 * Return: 0
 */
int main(void)
{
	int x;

		for (x = 0; x < 10; x++)
			printf("%d", x);
		printf("\n");
		return (0);
}
